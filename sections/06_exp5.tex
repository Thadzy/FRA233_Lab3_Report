% !TeX root = ../main.tex

\section{การทดลองที่ 5: 2-State KF --- ออกแบบ, Implement, และทดสอบ Free Vibration}

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{บทนำ}

การทดลองที่ 1--4 ได้พารามิเตอร์ครบสำหรับการสร้างแบบจำลองระบบ Mass-Spring-Damper
ได้แก่ $K = 32.04$~N/m จากการทดลองที่ 4 (Setup มี Guide Rail), มวล $m = 0.290$~kg
จากการชั่ง และ Damping Coefficient $C = 0.3882$~N$\cdot$s/m จาก Parameter Estimator
ในการทดลองที่ 4 การทดลองที่ 5 จึงเป็นการนำพารามิเตอร์เหล่านี้ทั้งหมดมาบูรณาการ
เพื่อสร้าง \textbf{2-State Discrete Kalman Filter} ที่สมบูรณ์

เป้าหมายหลักของ 2-State KF คือการประมาณค่า State สองตัวพร้อมกัน ได้แก่
\textbf{Position} $x_1$ ที่วัดได้โดยตรงจาก HC-SR04 แต่มี Noise
และ \textbf{Velocity} $x_2$ ที่ \textit{ไม่สามารถวัดได้โดยตรง} ด้วย Sensor
ความสามารถนี้คือสิ่งที่ทำให้ Kalman Filter มีคุณค่าในระบบควบคุมจริง
เพราะระบบสามารถรู้ ความเร็วของมวลได้โดยไม่ต้องติดตั้ง Velocimeter เพิ่ม

ในการทดลองนี้จะพิสูจน์ว่า 2-State KF สามารถ (1) ประมาณ Position ได้เรียบกว่า
Raw Sensor Data, (2) ประมาณ Velocity ได้อย่างสมเหตุสมผลทางกายภาพ,
และ (3) ให้ผลที่สอดคล้องกับ Mathematical Model ที่ได้จากการทดลองที่ 4

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{วัตถุประสงค์}

\begin{itemize}
    \item เพื่อสร้าง 2-State (Position + Velocity) Discrete Kalman Filter
          สำหรับระบบ Mass-Spring-Damper บน STM32 Nucleo G474RE
    \item เพื่อ Discretize State-Space Model ($A_c$, $B_c$, $H$)
          และเลือก Sampling Rate ที่เหมาะสมพร้อมเหตุผลทางทฤษฎี
    \item เพื่อกำหนดค่า Matrix $Q$ (2$\times$2) และ Scalar $R$
          พร้อมอธิบายความหมายทางกายภาพของแต่ละ Element
    \item เพื่อทดสอบ KF ด้วย Free Vibration ที่ Initial Displacement
          ต่างกัน 3 ระดับ และเปรียบเทียบกับ Mathematical Model
    \item เพื่อวิเคราะห์ว่า Velocity Estimate ที่ KF คำนวณได้
          มีความสมเหตุสมผลทางกายภาพหรือไม่
\end{itemize}

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{สมมติฐาน}

\begin{itemize}
    \item 2-State KF จะให้ค่าประมาณ Position ที่เรียบกว่า Raw Data
          โดยยังสอดคล้องกับ Mathematical Model ที่ Identify ได้
    \item Velocity Estimate จาก KF (ที่ไม่ได้วัดโดยตรง) จะมีความสอดคล้อง
          ทางกายภาพกับ Derivative ของ Displacement โดยเฉพาะอย่างยิ่ง
          Velocity ควรเป็นศูนย์ที่จุด Maximum Displacement
          และสูงสุดที่จุด Equilibrium Crossing
    \item $Q$ ที่ใหญ่ขึ้นจะทำให้ KF ติดตาม Measurement มากขึ้น
          ส่วน $R$ ที่ใหญ่ขึ้นจะทำให้ KF พึ่ง Model Prediction มากขึ้น
    \item ที่ Displacement ขนาดใหญ่ ($x_0 = 6$~cm) Linear Model
          อาจเบี่ยงเบนจาก Response จริงเนื่องจากความไม่เชิงเส้นของสปริง
          และแรงเสียดทาน
\end{itemize}

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{ตัวแปร}

\begin{itemize}
    \item \textbf{ตัวแปรอิสระ:} Initial Release Displacement $x_0$
          ที่ 2~cm, 4~cm, และ 6~cm; ค่า Matrix $Q$ ที่ปรับเปลี่ยนใน
          Sensitivity Study; ค่า Scalar $R$ ที่ปรับเปลี่ยน
    \item \textbf{ตัวแปรตาม:} ค่าประมาณ Position $\hat{x}_1$ (cm),
          ค่าประมาณ Velocity $\hat{x}_2$ (cm/s),
          Estimation Error ระหว่าง KF Output กับ Raw Measurement,
          ความสอดคล้องระหว่าง KF Output กับ Mathematical Model
    \item \textbf{ตัวแปรควบคุม:} ค่า $m = 0.290$~kg, $K = 32.04$~N/m,
          $C = 0.3882$~N$\cdot$s/m คงที่จากการทดลองที่ 1 และ 4,
          Sampling Time $T_s = 10$~ms คงที่,
          Control Input $u = 0$ (Free Vibration)
\end{itemize}

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{เอกสารและงานวิจัยที่เกี่ยวข้อง}

\subsubsection{ค่า Spring Constant ที่ใช้ในแต่ละการทดลอง}

เนื่องจากการทดลองที่~4 และ~5 ใช้ Setup ที่เพิ่ม Guide Rail
เข้ามาเพื่อบังคับให้มวลเคลื่อนที่ในแนวดิ่งอย่างเคร่งครัด
ทำให้ค่า $K$ ที่ Estimate ได้ต่างจากการทดลองที่~1
ตารางที่~\ref{tab:K_summary} สรุปค่า $K$ ที่ใช้ในแต่ละการทดลอง

\begin{table}[htbp]
\centering
\caption{ค่า Spring Constant $K$ ที่ใช้ในแต่ละการทดลอง}
\label{tab:K_summary}
\begin{tabular}{clcc}
\toprule
การทดลอง & วิธีได้มา & $K$ (N/m) & เหตุผล \\
\midrule
1 & Full OLS Regression & 29.11 & Setup ไม่มี Guide Rail \\
4 & MATLAB Parameter Estimator & 32.04 & Setup มี Guide Rail \\
5 & ใช้ค่าจากการทดลองที่~4 & 32.04 & ใช้ Setup เดียวกัน \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{State-Space Representation ของ Second-Order System}

สมการ EOM ของ MSD เป็น Second-Order ODE ซึ่งสามารถแปลงเป็น
First-Order State-Space Form โดยกำหนด State Vector
$\mathbf{x} = [x_1,\; x_2]^T = [\text{position},\; \text{velocity}]^T$:

\begin{equation}
    \dot{x}_1 = x_2
    \label{eq:ss_x1dot}
\end{equation}
\begin{equation}
    \dot{x}_2 = -\frac{K}{m} x_1 - \frac{C}{m} x_2
    \label{eq:ss_x2dot}
\end{equation}

เหตุผลที่เลือก Position และ Velocity เป็น State Vector คือทั้งสองตัวแปร
ร่วมกันอธิบายพลศาสตร์ของระบบได้ครบถ้วน ทราบทั้งสองแล้วสามารถทำนาย
พฤติกรรมในอนาคตได้ทั้งหมด ซึ่งเป็นข้อกำหนดพื้นฐานของ State Variable

\subsubsection{Continuous-Domain State-Space Matrices}

จากสมการ~\eqref{eq:ss_x1dot}--\eqref{eq:ss_x2dot} เขียนในรูป Matrix ได้:

\begin{equation}
    \dot{\mathbf{x}}(t) = A_c \mathbf{x}(t) + B_c u(t), \qquad
    z(t) = H\mathbf{x}(t) + v(t)
    \label{eq:ss_compact}
\end{equation}

โดย

\begin{equation}
    A_c = \begin{bmatrix} 0 & 1 \\ -K/m & -C/m \end{bmatrix}, \quad
    B_c = \begin{bmatrix} 0 \\ 1/m \end{bmatrix}, \quad
    H   = \begin{bmatrix} 1 & 0 \end{bmatrix}
    \label{eq:cont_matrices}
\end{equation}

\subsubsection{เหตุผลที่ $H = [1,\; 0]$}

HC-SR04 วัดได้เฉพาะ Position ($x_1$) ไม่สามารถวัด Velocity ($x_2$) ได้โดยตรง
ดังนั้น Observation Matrix จึงต้อง``เลือก''เฉพาะ Element แรกของ State Vector:

\begin{equation}
    z_k = H\mathbf{x}_k + v_k = \begin{bmatrix}1 & 0\end{bmatrix}
          \begin{bmatrix}x_1 \\ x_2\end{bmatrix} + v_k = x_1 + v_k
    \label{eq:obs_equation}
\end{equation}

KF จัดการกับ \textbf{Partial Observation} นี้โดยใช้ $A_d$ 
ข้อมูล Velocity ผ่านรอบเวลา Prediction Step จะแพร่ข้อมูล Position
ไปยัง Velocity ผ่านองค์ประกอบ $(A_d)_{21}$ ทำให้แม้จะวัดได้แค่ Position
KF ก็สามารถ Estimate Velocity ได้ผ่านกระบวนการ Predict-Update Loop

\subsubsection{เหตุผลที่ $u = 0$ ใน Free Vibration}

เมื่อใช้การกระจัดจาก Equilibrium เป็น State แรงโน้มถ่วง $mg$
ถูก Balance โดย Spring Pre-load $Kx_\text{eq} = mg$ พอดี
ทำให้ EOM กลายเป็น $m\ddot{x} + C\dot{x} + Kx = 0$
ซึ่งไม่มี External Input ดังนั้น $u = 0$ และ $B_d = \mathbf{0}$
ตลอดการทดลอง Free Vibration

\subsubsection{การเลือก Sampling Rate}

ตาม Nyquist-Shannon Sampling Theorem Sampling Frequency $f_s$
ต้องสูงกว่า 2 เท่าของ Natural Frequency สูงสุดในระบบ
แต่สำหรับ Control System ในทางปฏิบัติควรใช้ 10--20 เท่าเพื่อความเสถียร:

\begin{equation}
    f_s \geq 20\, f_n, \qquad \text{หรือ} \qquad
    T_s \leq \frac{1}{20\, f_n} = \frac{2\pi}{20\,\omega_n}
    \label{eq:sampling_criterion}
\end{equation}

โดยที่ $f_n = \omega_n / (2\pi)$ คือ Natural Frequency ในหน่วย Hz
เหตุผลที่ใช้ 20 เท่าแทน 2 เท่า เพราะต้องการให้สัญญาณที่ Discretize แล้ว
ยังคงจำลองพลศาสตร์ Continuous ได้อย่างแม่นยำ
และ Euler Forward Approximation จะมี Truncation Error น้อยลง

\subsubsection{Discretization: Euler Forward vs.\ ZOH}

\paragraph{Euler Forward (First-Order Approximation)}

\begin{equation}
    A_d = I + A_c T_s =
    \begin{bmatrix} 1 & T_s \\ -\dfrac{K}{m}T_s & 1 - \dfrac{C}{m}T_s \end{bmatrix}
    \label{eq:euler_Ad}
\end{equation}

\begin{equation}
    B_d = B_c T_s = \begin{bmatrix} 0 \\ T_s/m \end{bmatrix}
    \label{eq:euler_Bd}
\end{equation}

วิธีนี้ง่ายในการ Implement แต่มี Truncation Error $O(T_s^2)$
ที่ $T_s = 10$~ms ค่า $A_d$ จาก Euler Forward ต่างจาก ZOH ประมาณ
$2\%$ ซึ่งยอมรับได้สำหรับ Application นี้

\paragraph{Zero-Order Hold (ZOH)}

\begin{equation}
    A_d = e^{A_c T_s} = I + A_c T_s + \frac{(A_c T_s)^2}{2!} + \cdots
    \label{eq:zoh_Ad}
\end{equation}

วิธีนี้แม่นยำกว่าและรักษา Eigenvalue ของระบบ Continuous ไว้ได้ดีกว่า
แต่ต้องคำนวณ Matrix Exponential ซึ่งซับซ้อนกว่าสำหรับ Embedded System

% ทั้งสองวิธีให้ผล $A_d$ ที่ Stable (Eigenvalue อยู่ภายใน Unit Circle)
% สำหรับพารามิเตอร์ในการทดลองนี้

\subsubsection{ความหมายทางกายภาพของ Matrix $Q$ และ Scalar $R$}

\paragraph{Measurement Noise Covariance $R$ (Scalar)}

$R$ คือ Variance ของ Noise ของ HC-SR04 ที่คำนวณจากข้อมูลการทดลองที่ 2
โดยวัด 5 ชุดที่ระยะ 20~cm แล้วนำ Variance มาเฉลี่ย:

\begin{equation}
    R = \bar{\sigma}^2_\text{sensor} = 3.37 \times 10^{-6}\ \text{m}^2
    \quad (= 0.0337\ \text{cm}^2)
    \label{eq:R_exp5_nominal}
\end{equation}

$R$ บอกว่า Filter ควรเชื่อถือ Measurement มากแค่ไหน
ค่าสูงหมายความว่า Sensor Noisy มาก Filter จะพึ่ง Model Prediction มากขึ้น

\paragraph{Process Noise Matrix $Q$ (2$\times$2)}

$Q$ เป็น Diagonal Matrix ที่แสดงความไม่แน่นอนในแต่ละ State:
\begin{equation}
    Q = \begin{bmatrix} q_1 & 0 \\ 0 & q_2 \end{bmatrix}
    \label{eq:Q_structure}
\end{equation}

ความหมายทางกายภาพของแต่ละ Element:
\begin{itemize}
    \item \textbf{$q_1 = Q_{11}$ (Position Process Noise):}
          ความไม่แน่นอนในการทำนาย Position ในแต่ละ Step
          ค่านี้มักมีขนาดเล็กเพราะ Position เปลี่ยนแปลงช้า
          (เป็น Integral ของ Velocity) และ Dynamic Model
          อธิบาย Position ได้แม่นยำในช่วง Displacement เล็ก

    \item \textbf{$q_2 = Q_{22}$ (Velocity Process Noise):}
          ความไม่แน่นอนในการทำนาย Velocity ในแต่ละ Step
          ค่านี้มักใหญ่กว่า $q_1$ เพราะ Velocity ได้รับผลกระทบ
          จากแรงที่ Model ไม่ได้รวม เช่น แรงเสียดทาน Non-linear
          และ Air Resistance ซึ่งทำให้ Model Prediction ของ Velocity
          มีความคลาดเคลื่อนสะสมมากกว่า

    \item \textbf{Off-diagonal elements = 0:}
          สมมติว่า Process Noise ของ Position และ Velocity
          ไม่มี Correlation กัน (Statistically Independent)
          ซึ่งเป็นการประมาณที่สมเหตุสมผลและลดความซับซ้อนในการ Tune
\end{itemize}

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{ทฤษฎี: 2-State Discrete Kalman Filter สำหรับ MSD}

\subsubsection{Continuous State-Space Model}

กำหนด State Vector:
\begin{equation}
    \mathbf{x}(t) = \begin{bmatrix} x_1(t) \\ x_2(t) \end{bmatrix}
                  = \begin{bmatrix} \text{Displacement จาก Equilibrium (m)} \\
                                    \text{Velocity (m/s)} \end{bmatrix}
    \label{eq:state_vector}
\end{equation}

จาก EOM ของ MSD แปลงเป็น First-Order Form:
\begin{equation}
    \dot{\mathbf{x}}(t) = A_c \mathbf{x}(t) + B_c u(t), \qquad
    z(t) = H \mathbf{x}(t) + v(t)
    \label{eq:ss_cont}
\end{equation}

\begin{equation}
    A_c = \begin{bmatrix} 0 & 1 \\ -K/m & -C/m \end{bmatrix}, \quad
    B_c = \begin{bmatrix} 0 \\ 1/m \end{bmatrix}, \quad
    H   = \begin{bmatrix} 1 & 0 \end{bmatrix}
    \label{eq:ss_matrices}
\end{equation}

\subsubsection{สมการ 2-State Discrete Kalman Filter ทั้ง 5 สมการ}

กำหนด $\hat{\mathbf{x}}_k = [\hat{x}_1(k),\; \hat{x}_2(k)]^T$
= $[\text{estimated position},\; \text{estimated velocity}]^T$

\paragraph{ขั้นตอน Prediction (A Priori)}

\begin{equation}
    \hat{\mathbf{x}}_k^{(-)} = A_d\,\hat{\mathbf{x}}_{k-1}
    \tag{2-State-1: State Prediction}
    \label{eq:kf2_predict_state}
\end{equation}

\begin{equation}
    P_k^{(-)} = A_d\,P_{k-1}\,A_d^T + Q
    \tag{2-State-2: Covariance Prediction}
    \label{eq:kf2_predict_cov}
\end{equation}

\paragraph{ขั้นตอน Update (A Posteriori)}

\begin{equation}
    \mathbf{K}_k = P_k^{(-)} H^T \!\left(H P_k^{(-)} H^T + R\right)^{-1}
    \tag{2-State-3: Kalman Gain}
    \label{eq:kf2_gain}
\end{equation}

\begin{equation}
    \hat{\mathbf{x}}_k = \hat{\mathbf{x}}_k^{(-)}
                        + \mathbf{K}_k\!\left(z_k - H\hat{\mathbf{x}}_k^{(-)}\right)
    \tag{2-State-4: State Update}
    \label{eq:kf2_update_state}
\end{equation}

\begin{equation}
    P_k = \left(I - \mathbf{K}_k H\right) P_k^{(-)}
    \tag{2-State-5: Covariance Update}
    \label{eq:kf2_update_cov}
\end{equation}

ขนาดของ Matrix และ Vector ในระบบนี้:

\begin{table}[htbp]
    \centering
    \caption{ขนาดของ Matrix และ Vector ใน 2-State KF}
    \label{tab:matrix_dims}
    \begin{tabular}{clcl}
        \hline
        \textbf{สัญลักษณ์} & \textbf{ชื่อ} & \textbf{ขนาด} & \textbf{หน่วย} \\
        \hline
        $\hat{\mathbf{x}}_k$ & State Estimate     & $2\times1$ & [m, m/s] \\
        $A_d$                & State Transition   & $2\times2$ & --- \\
        $P_k$                & Error Covariance   & $2\times2$ & --- \\
        $Q$                  & Process Noise      & $2\times2$ & --- \\
        $H$                  & Observation Matrix & $1\times2$ & --- \\
        $\mathbf{K}_k$       & Kalman Gain        & $2\times1$ & --- \\
        $R$                  & Measurement Noise  & Scalar     & m$^2$ \\
        $z_k$                & Measurement        & Scalar     & m \\
        \hline
    \end{tabular}
\end{table}

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{ขั้นตอนการทดลอง}

\subsubsection{ส่วน A --- Derive และ Discretize Model}

\begin{enumerate}
    \item เขียน Continuous State-Space Model แทนค่า $K = 32.04$~N/m,
          $C = 0.3882$~N$\cdot$s/m, $m = 0.290$~kg
          คำนวณ $\omega_n$ และ $f_n$
    \item เลือก $T_s$ ที่ตอบสนองเงื่อนไข $T_s \leq 1/(20 f_n)$
          และสอดคล้องกับ Sampling Time ของ HC-SR04
    \item คำนวณ $A_d$ ด้วย Euler Forward
          ตรวจสอบว่า Eigenvalue ของ $A_d$ อยู่ภายใน Unit Circle (Stable)
    \item เขียนสมการ KF ทั้ง 5 สมการในรูปแบบ Matrix
\end{enumerate}

\subsubsection{ส่วน B --- การกำหนดค่า $R$ และ Matrix $Q$}

\paragraph{Measurement Noise Covariance $R$}
ใช้ค่าเดียวกับที่คำนวณได้จากการทดลองที่~2 ที่ระยะ 20~cm:
\begin{equation}
    R = 3.37 \times 10^{-6}\ \text{m}^2
    \label{eq:R_exp5_measured}
\end{equation}

\paragraph{Process Noise Matrix $Q$}
กำหนด $Q$ เป็น Diagonal Matrix ขนาด $2\times2$ โดยอาศัยหลักการดังนี้
\begin{itemize}
    \item $q_1$ (Position Process Noise) ตั้งค่าน้อยกว่า $q_2$
          เนื่องจาก Position เปลี่ยนแปลงช้าและ Dynamic Model อธิบายได้แม่นยำ
    \item $q_2$ (Velocity Process Noise) ตั้งค่าสูงกว่า $q_1$
          เนื่องจาก Velocity ได้รับผลกระทบจาก Non-linear Friction จาก Guide Rail
\end{itemize}

ค่า Baseline ที่ได้จากการ Tune:
\begin{equation}
    Q_\text{baseline} = \begin{bmatrix} 1\times10^{-7} & 0 \\ 0 & 1\times10^{-5} \end{bmatrix}
    \ \text{m}^2
    \label{eq:Q_baseline}
\end{equation}

อัตราส่วน $Q/R$ ที่ Baseline:
\begin{equation}
    \frac{q_1}{R} = \frac{1\times10^{-7}}{3.37\times10^{-6}} \approx 0.030, \qquad
    \frac{q_2}{R} = \frac{1\times10^{-5}}{3.37\times10^{-6}} \approx 2.967
    \label{eq:QR_ratio}
\end{equation}

ค่า $q_1/R < 1$ หมายความว่า Filter เชื่อ Model Prediction ของ Position มากกว่า Sensor
ส่วน $q_2/R > 1$ หมายความว่า Filter เชื่อ Sensor มากกว่า Model ในส่วนของ Velocity

\subsubsection{ส่วน C --- Implement 2-State KF บน STM32}

\begin{lstlisting}[language=C,
    caption={\texttt{kalman.h} --- 2-State Discrete Kalman Filter Public Interface},
    label={lst:kalmanh_exp5}]
#ifndef KALMAN_H
#define KALMAN_H

typedef struct {
    float dt;           /* Sampling period (s)                  */
    float q0, q1;       /* Process noise: position, velocity    */
    float R;            /* Measurement noise variance (m^2)     */
    float x0, x1;       /* State estimates: position, velocity  */
    float p00, p01, p11;/* Error covariance (symmetric 2x2)     */
} Kalman2State_t;

void  Kalman2_Init(Kalman2State_t *kf, float dt, float q0, float q1,
                   float R, float pos, float vel);
float Kalman2_Update(Kalman2State_t *kf, float measurement);
float Kalman2_GetVelocity(const Kalman2State_t *kf);

#endif /* KALMAN_H */
\end{lstlisting}

\begin{lstlisting}[language=C,
    caption={\texttt{kalman.c} --- 2-State Discrete Kalman Filter Implementation
             พร้อม MSD Physics-Based Prediction},
    label={lst:kalmanc_exp5}]
#include "kalman.h"

/* Physical parameters from Lab 1 and Lab 4 */
static const float M_KG  = 0.290f;   /* Mass (kg)                    */
static const float K_NM  = 32.04f;   /* Spring constant (N/m)        */
static const float C_NSM = 0.3882f;  /* Damping coefficient (N.s/m)  */

void Kalman2_Init(Kalman2State_t *kf, float dt, float q0, float q1,
                  float R, float pos, float vel)
{
    kf->dt  = dt;
    kf->q0  = q0;
    kf->q1  = q1;
    kf->R   = R;
    kf->x0  = pos;  /* initial position (m from equilibrium) */
    kf->x1  = vel;  /* initial velocity (m/s), typically 0   */
    /* Initial error covariance P = I (high initial uncertainty) */
    kf->p00 = 1.0f;
    kf->p01 = 0.0f;
    kf->p11 = 1.0f;
}

float Kalman2_Update(Kalman2State_t *kf, float measurement)
{
    const float dt = kf->dt;
    const float w2 = K_NM / M_KG;    /* omega_n^2 = K/m */
    const float z2 = C_NSM / M_KG;   /* 2*zeta*omega_n = C/m */

    float accel  = (-w2 * kf->x0) - (z2 * kf->x1);
    float x0_p   = kf->x0 + kf->x1 * dt + 0.5f * accel * dt * dt;
    float x1_p   = kf->x1 + accel * dt;

    float a00 = 1.0f,      a01 = dt;
    float a10 = -w2 * dt,  a11 = 1.0f - z2 * dt;

    /* P_pred = A * P * A^T + Q */
    float ap00 = a00 * kf->p00 + a01 * kf->p01;
    float ap01 = a00 * kf->p01 + a01 * kf->p11;
    float ap10 = a10 * kf->p00 + a11 * kf->p01;
    float ap11 = a10 * kf->p01 + a11 * kf->p11;

    float p00_p = ap00 * a00 + ap01 * a01 + kf->q0;
    float p01_p = ap00 * a10 + ap01 * a11;
    float p11_p = ap10 * a10 + ap11 * a11 + kf->q1;

    float S          = p00_p + kf->R;
    float K0         = p00_p / S;      /* Kalman gain for position */
    float K1         = p01_p / S;      /* Kalman gain for velocity */
    float innovation = measurement - x0_p;

    kf->x0  = x0_p + K0 * innovation;
    kf->x1  = x1_p + K1 * innovation;

    /* P = (I - K*H) * P^- */
    kf->p00 = (1.0f - K0) * p00_p;
    kf->p01 = (1.0f - K0) * p01_p;
    kf->p11 = p11_p - K1 * p01_p;

    return kf->x0;
}

float Kalman2_GetVelocity(const Kalman2State_t *kf)
{
    return kf->x1;
}
\end{lstlisting}

\begin{lstlisting}[language=C,
    caption={\texttt{main.c} --- 2-State KF สำหรับระบบ MSD (Free Vibration)},
    label={lst:main_exp5}]
/* Sampling period — must match HAL_Delay at bottom of loop */
#define KALMAN_DT          0.010f

/* Process noise (tuned from Sensitivity Study, baseline values) */
#define KALMAN_Q0          1e-7f     /* Position process noise (m^2) */
#define KALMAN_Q1          1e-5f     /* Velocity process noise        */

/* Measurement noise: mean variance from Lab 2 (5 runs at 20 cm)    */
/* Raw value: 3.3719 mm^2 -> converted to m^2: 3.3719e-6 m^2        */
#define KALMAN_R           3.37e-6f

/* Equilibrium distance read by sensor when mass is at rest (cm) */
#define EQUILIBRIUM_CM     11.3f

/* ... peripheral init (HAL_Init, SystemClock, GPIO, TIM1, UART) ... */

HAL_TIM_Base_Start(&htim1);
HCSR04_Init(&htim1);

/* Seed: take first reading, convert to displacement from equilibrium */
float raw_cm     = HCSR04_Read();
if (raw_cm < 0.0f) raw_cm = EQUILIBRIUM_CM;
float init_disp  = (raw_cm - EQUILIBRIUM_CM) / 100.0f;  /* m */

Kalman2_Init(&kf_distance, KALMAN_DT, KALMAN_Q0, KALMAN_Q1,
             KALMAN_R, init_disp, 0.0f);

while (1) {
    raw_cm = HCSR04_Read();
    if (raw_cm >= 0.0f) {
        /* Convert absolute reading to displacement from equilibrium */
        float disp_m = (raw_cm - EQUILIBRIUM_CM) / 100.0f;

        float pos_m  = Kalman2_Update(&kf_distance, disp_m);
        float vel_ms = Kalman2_GetVelocity(&kf_distance);

        /* Output: timestamp(ms), raw(cm), filtered(m), velocity(m/s) */
        printf("%lu,%.4f,%.4f,%.4f\r\n",
               HAL_GetTick(), raw_cm, pos_m, vel_ms);
        fflush(stdout);
    }
    HAL_Delay(10);  /* Ts = 10 ms */
}
\end{lstlisting}

\subsubsection{ส่วน D --- ทดสอบ Free Vibration}

\begin{enumerate}
    \setcounter{enumi}{8}
    \item กำหนด $x = 0$ ที่ Equilibrium ดึงมวลไปที่ $x_0 = 2$~cm แล้วปล่อย
          บันทึก 10 วินาที (Raw $z_k$, Position Estimate, Velocity Estimate)
          ทำซ้ำ 3 ครั้ง บันทึกเป็น \texttt{run1}--\texttt{run3}
    \item ทำซ้ำที่ $x_0 = 4$~cm และ $x_0 = 6$~cm
    \item Simulate MSD Model จากการทดลองที่ 4 ใน MATLAB
          ด้วย Initial Condition เดียวกัน เปรียบเทียบกับ KF Output
\end{enumerate}

\subsubsection{ส่วน E --- Sensitivity Study}

\begin{enumerate}
    \setcounter{enumi}{11}
    \item ที่ $x_0 = 6$~cm ทดสอบ $Q$ อย่างน้อย 3 ชุด
          (Scale Diagonal ด้วย $0.01\times$, $1\times$, $100\times$)
    \item ปรับ $R$ ($0.5\times$, $1\times$, $2\times$ ของ Measured Variance)
          บันทึกการเปลี่ยนแปลงของ Position Estimate
\end{enumerate}

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{ผลการทดลอง}

\subsubsection{Continuous State-Space Matrices}

แทนค่าพารามิเตอร์จริงลงใน Matrix $A_c$, $B_c$, $H$:

\begin{equation}
    A_c = \begin{bmatrix} 0 & 1 \\ -K/m & -C/m \end{bmatrix}
        = \begin{bmatrix} 0 & 1 \\ -110.4828 & -1.3386 \end{bmatrix},
    \quad
    B_c = \begin{bmatrix} 0 \\ 1/m \end{bmatrix}
        = \begin{bmatrix} 0 \\ 3.4483 \end{bmatrix},
    \quad
    H = \begin{bmatrix} 1 & 0 \end{bmatrix}
    \label{eq:cont_matrices_numerical}
\end{equation}

\subsubsection{Sampling Rate และ Discrete State-Space Matrices}

คำนวณ Natural Frequency จากพารามิเตอร์จริง:

\begin{equation}
    \omega_n = \sqrt{\frac{K}{m}} = \sqrt{\frac{32.04}{0.290}} = 10.511\ \text{rad/s},
    \qquad
    f_n = \frac{\omega_n}{2\pi} = \frac{10.511}{2\pi} = 1.673\ \text{Hz}
    \label{eq:omega_n_numerical}
\end{equation}

ตรวจสอบเงื่อนไข Sampling:
\begin{equation}
    T_s \leq \frac{1}{20 f_n} = \frac{1}{20 \times 1.673} = 29.89\ \text{ms}
    \quad \Rightarrow \quad \text{เลือก } T_s = 10\ \text{ms} \quad \checkmark
    \label{eq:ts_choice}
\end{equation}

$T_s = 10$~ms ตอบสนองเงื่อนไขและสอดคล้องกับ HC-SR04 ที่ต้องการ
เวลาในการวัดแต่ละครั้งอย่างน้อย 60~ms ทำให้ได้ Sampling Rate ที่มีความเสถียรและ
ป้องกัน Aliasing ของสัญญาณ Oscillation ที่ $f_n = 1.673$~Hz

Discretize ด้วย Euler Forward ที่ $T_s = 0.010$~s:

\begin{equation}
    A_d = I + A_c T_s
        = \begin{bmatrix} 1 & T_s \\ -\frac{K}{m}T_s & 1-\frac{C}{m}T_s \end{bmatrix}
        = \begin{bmatrix} 1.000000 & 0.010000 \\ -1.104828 & 0.986614 \end{bmatrix}
    \label{eq:Ad_result}
\end{equation}

\begin{equation}
    B_d = \mathbf{0}_{2\times1}
    \qquad (u = 0\ \text{สำหรับ Free Vibration})
    \label{eq:Bd_result}
\end{equation}

ตรวจสอบ Stability ของ $A_d$: Eigenvalue ของ $A_d$ คือ
$\lambda_{1,2} = 0.9933 \pm 0.1049j$
ซึ่งมี $|\lambda| = 0.9988 < 1$ ยืนยันว่าระบบ Discrete Stable

\begin{table}[htbp]
    \centering
    \caption{ค่าพารามิเตอร์ KF ที่ใช้ในการทดสอบ}
    \label{tab:kf2_params}
    \begin{tabular}{lccc}
        \hline
        \textbf{พารามิเตอร์} & \textbf{สัญลักษณ์} & \textbf{หน่วย} & \textbf{ค่า} \\
        \hline
        Position Process Noise  & $q_1$  & m$^2$       & $1 \times 10^{-7}$ \\
        Velocity Process Noise  & $q_2$  & ---         & $1 \times 10^{-5}$ \\
        Measurement Noise       & $R$    & m$^2$       & $3.37 \times 10^{-6}$ \\
        Sampling Period         & $T_s$  & ms          & $10$ \\
        Equilibrium Distance    & $x_\text{eq}$ & cm   & $11.3$ \\
        Initial Position Estimate & $\hat{x}_1(0)$ & m & $z_0 - x_\text{eq}$ (การวัดแรก) \\
        Initial Velocity Estimate & $\hat{x}_2(0)$ & m/s & $0$ \\
        Initial Covariance      & $P_0$  & ---         & $I_{2\times2}$ \\
        \hline
    \end{tabular}
\end{table}

\subsubsection{Free Vibration ที่ระยะต่างกัน}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{images/fig1_freevib_positions.png}
    \caption{Free Vibration ที่ $x_0 = 2$, 4, 6~cm: Raw $z_k$ (จุดแดง)
             และ KF Position Estimate (เส้นน้ำเงิน) บนแกนเดียวกัน
             (แกน $x$: เวลา (s), แกน $y$: ระยะทาง (cm))}
    \label{fig:exp5_freevib_positions}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{images/fig2_freevib_velocity.png}
    \caption{KF Velocity Estimate $\hat{x}_2$ ที่ $x_0 = 2$, 4, 6~cm
             แสดงให้เห็น Pattern การสั่นที่ Amplitude ต่างกัน
             (แกน $x$: เวลา (s), แกน $y$: ความเร็ว (cm/s))}
    \label{fig:exp5_freevib_velocity}
\end{figure}

จากการวิเคราะห์ข้อมูลที่เก็บได้ (run3 ที่ $x_0 = 6$~cm เป็นตัวอย่าง)
ตาราง~\ref{tab:exp5_noise_reduction} แสดง Noise Reduction ที่ได้จาก KF
เทียบกับ Raw Sensor Data:

\begin{table}[htbp]
    \centering
    \caption{Noise Reduction ของ KF ที่ระยะ Release ต่างกัน}
    \label{tab:exp5_noise_reduction}
    \begin{tabular}{cccc}
        \hline
        \textbf{ระยะ $x_0$} & \textbf{Raw Variance (cm$^2$)} &
        \textbf{Filtered Variance (cm$^2$)} & \textbf{Noise Reduction (\%)} \\
        \hline
        2~cm & 0.1616 & 0.1162 & 28.1 \\
        4~cm & 0.8702 & 0.6970 & 19.9 \\
        6~cm & 1.5266 & 1.2726 & 16.6 \\
        \hline
    \end{tabular}
\end{table}

Noise Reduction ลดลงเมื่อ Displacement ใหญ่ขึ้น เพราะ Signal Amplitude
ที่ใหญ่ขึ้นบังคับให้ KF ต้องตาม Oscillation อย่างรวดเร็ว จึง Smooth ได้น้อยลงสัดส่วน
ซึ่งเป็นพฤติกรรมปกติของ KF ที่มี Fixed Q และ R

\subsubsection{Sensitivity Study: ผลกระทบของ $Q$ และ $R$}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{images/fig5_Q_overlay.png}
    \caption{Sensitivity Study ของ $Q$: KF Position Output สำหรับ
             $Q \times 0.01$, $Q \times 1$ (baseline), $Q \times 100$
             บนแกนเดียวกัน ที่ $x_0 = 6$~cm}
    \label{fig:exp5_Q_sensitivity}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{images/fig6_R_overlay.png}
    \caption{Sensitivity Study ของ $R$: KF Position Output สำหรับ
             $R \times 0.5$, $R \times 1$ (baseline), $R \times 2$
             บนแกนเดียวกัน ที่ $x_0 = 6$~cm}
    \label{fig:exp5_R_sensitivity}
\end{figure}

\begin{table}[htbp]
    \centering
    \caption{ผล Sensitivity Study: Noise Reduction (\%) สำหรับ $Q$ และ $R$ ต่างๆ}
    \label{tab:exp5_sensitivity}
    \begin{tabular}{llccc}
        \hline
        \textbf{การทดลอง} & \textbf{ค่าที่ปรับ} &
        \textbf{Raw Var (cm$^2$)} & \textbf{Filt Var (cm$^2$)} &
        \textbf{Reduction (\%)} \\
        \hline
        Q Sensitivity & $Q \times 0.01$ & 1.8659 & 0.1827 & \textbf{90.2} \\
                      & $Q \times 1$ (baseline) & 1.5266 & 1.2726 & 16.6 \\
                      & $Q \times 100$ & 2.7326 & 2.7525 & $-$0.7 \\
        \hline
        R Sensitivity & $R \times 0.5$ & 2.2723 & 2.2928 & $-$0.9 \\
                      & $R \times 1$ (baseline) & 1.5266 & 1.2726 & 16.6 \\
                      & $R \times 2$ & 2.0366 & 1.6934 & 16.8 \\
        \hline
    \end{tabular}
\end{table}

\clearpage

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{สรุปผลการทดลอง}

\begin{itemize}
    \item 2-State KF สามารถประมาณค่าทั้ง Position และ Velocity ได้พร้อมกัน
          โดย Position Estimate เรียบกว่า Raw Data อย่างมีนัยสำคัญ
          (Noise Reduction 16.6--28.1\% ขึ้นกับ Amplitude)
          และ Velocity Estimate สอดคล้องทางกายภาพกับ Derivative ของ Displacement

    \item Velocity Estimate มีความสมเหตุสมผลทางกายภาพ:
          ค่า $|\hat{x}_2|$ สูงสุดที่ได้จากการทดลองที่ $x_0 = 6$~cm
          คือ 68.59~cm/s ซึ่งใกล้เคียงกับค่าทางทฤษฎี
          $v_\text{max} = x_0 \omega_n = 6 \times 10.511 = 63.07$~cm/s
          ความต่าง 8.7\% เกิดจาก Noise และ Transient ช่วงแรก

    \item ผล Sensitivity Study ยืนยันว่า $Q$ มีอิทธิพลต่อพฤติกรรม KF
          มากกว่า $R$ อย่างชัดเจน: การเปลี่ยน $Q \times 0.01$ ทำให้
          Noise Reduction เพิ่มจาก 16.6\% เป็น 90.2\%
          ส่วนการเปลี่ยน $R$ ในช่วง $0.5\times$--$2\times$ แทบไม่ส่งผลต่อ
          Noise Reduction เพราะ Q/R Ratio เปลี่ยนน้อย

    \item ที่ $Q \times 100$ และ $R \times 0.5$ Noise Reduction ติดลบ
          ($-$0.7\% และ $-$0.9\%) แสดงว่า Filter ไม่มีประโยชน์ในกรณีนั้น
          เนื่องจากการเชื่อ Measurement มากเกินไปทำให้ KF ดึงตาม Noise แทน

    \item ข้อจำกัดสำคัญ: Noise Floor ของ HC-SR04 ($\approx 2$~cm),
          ข้อสมมติ Linear Model, และ Non-Adaptive Filter
          ที่ $Q$ กับ $R$ คงที่ตลอดเวลา
\end{itemize}

%% ─────────────────────────────────────────────────────────────────────────────
\subsection{อภิปรายผล}

\subsubsection{ความหมายทางกายภาพของ Matrix $Q$ และ Scalar $R$}

\begin{itemize}
    \item \textbf{$Q_{11}$ (Position Process Noise) $= 1\times10^{-7}$~m$^2$:}
          แสดงความไม่แน่นอนใน Prediction ของ Position ในแต่ละ Step
          ค่านี้มีขนาดเล็กมากเมื่อเทียบกับ $R$ ($q_1/R \approx 0.030$)
          หมายความว่า Filter เชื่อ Dynamic Model มากกว่า Sensor Measurement
          ในส่วนของ Position ซึ่งสมเหตุสมผลเพราะ MSD Model อธิบาย Position
          ได้แม่นยำในช่วง Small Oscillation

    \item \textbf{$Q_{22}$ (Velocity Process Noise) $= 1\times10^{-5}$~m$^2$:}
          แสดงความไม่แน่นอนใน Prediction ของ Velocity ในแต่ละ Step
          ค่านี้ใหญ่กว่า $Q_{11}$ 100 เท่า และมีค่า $q_2/R \approx 2.967 > 1$
          หมายความว่า Filter เชื่อ Sensor มากกว่า Model ในส่วนของ Velocity
          เพราะ Velocity ได้รับผลกระทบจาก Non-linear Friction จาก Guide Rail
          ที่ Linear Model ไม่ครอบคลุม

    \item \textbf{Off-Diagonal Elements = 0:}
          สมมติว่า Process Noise ของ Position และ Velocity
          ไม่มี Correlation กัน Friction จาก Guide Rail เป็น Deterministic Force
          ที่ขึ้นกับความเร็ว ไม่ใช่ Stochastic Process Noise ที่ Random
          ดังนั้นผลของ Friction จึงถูก Absorb เข้าไปใน $q_2$ แทนที่จะสร้าง
          Cross-Covariance ระหว่าง State ทั้งสอง
          การตั้ง Off-diagonal เป็นศูนย์จึงเป็น Conservative Assumption
          ที่ยอมรับได้และลดความซับซ้อนในการ Tune

    \item \textbf{$R = 3.37\times10^{-6}$~m$^2$:}
          ได้จากการวัด Variance ของ HC-SR04 จริงในการทดลองที่ 2
          (Mean จาก 5 Run ที่ระยะ 20~cm แปลงจาก mm$^2$ เป็น m$^2$)
          ค่านี้สะท้อน Noise ที่แท้จริงของ Sensor ทำให้ Filter
          สามารถ Balance ระหว่าง Model และ Measurement ได้อย่างถูกต้อง
\end{itemize}

\subsubsection{เหตุผลที่ใช้ Displacement จาก Equilibrium เป็น State}

เมื่อใช้ Equilibrium เป็นจุดอ้างอิง แรงโน้มถ่วง $mg$ ถูก Balance
โดย Spring Pre-load $Kx_\text{eq} = mg$ พอดี ทำให้ EOM ไม่มี Constant Term:
\begin{equation}
    m\ddot{x} + C\dot{x} + Kx = 0
\end{equation}
ส่งผลให้ State-Space Model มีรูปแบบ $\dot{\mathbf{x}} = A_c\mathbf{x}$
ที่สะอาดโดยไม่มี Bias Term ใน Prediction Step
และ $u = 0$ ตลอดการทดลอง Free Vibration

\subsubsection{การวิเคราะห์ Velocity Estimate}

ตรวจสอบความสมเหตุสมผลของ Velocity Estimate ด้วย Energy Conservation:
\begin{equation}
    \frac{1}{2}Kx_0^2 = \frac{1}{2}mv_\text{max}^2
    \quad \Rightarrow \quad
    v_\text{max} = x_0\sqrt{\frac{K}{m}} = x_0\,\omega_n
    \label{eq:v_max_theory}
\end{equation}

\begin{table}[htbp]
    \centering
    \caption{เปรียบเทียบ Velocity สูงสุดระหว่างทฤษฎีและ KF Estimate}
    \label{tab:velocity_comparison}
    \begin{tabular}{cccc}
        \hline
        $x_0$ & $v_\text{max}$ ทฤษฎี (cm/s) & $|\hat{x}_2|_\text{max}$ จาก KF (cm/s) & ความต่าง \\
        \hline
        2~cm & 21.02 & --- & --- \\
        4~cm & 42.04 & --- & --- \\
        6~cm & 63.07 & 68.59 & 8.7\% \\
        \hline
    \end{tabular}
\end{table}

ความต่างที่ 8.7\% ที่ $x_0 = 6$~cm เกิดจากสาเหตุหลักสองประการ:
\begin{itemize}
    \item Transient ช่วงแรกที่ KF ยัง Converge ทำให้ Velocity Estimate
          ในช่วงแรกสูงเกินจริง
    \item ความไม่สมบูรณ์ของ Linear Model ที่ Displacement ใหญ่
          ทำให้ Energy Conservation ในรูป Linear ไม่สมบูรณ์
\end{itemize}

KF Velocity Estimate สมเหตุสมผลทางกายภาพในสามด้าน:
\begin{itemize}
    \item $\hat{x}_2 \approx 0$ ที่จุด Maximum Displacement (Peak ของ $z_k$)
    \item $|\hat{x}_2|$ สูงสุดที่ Equilibrium Crossing สอดคล้อง
          กับ Energy Conservation
    \item $\hat{x}_2$ มีเครื่องหมายสลับกันทุกครึ่ง Period
          สอดคล้องกับการแกว่งของระบบ
\end{itemize}

\subsubsection{ผลกระทบของ $Q/R$ Ratio ต่อพฤติกรรม KF}

จากผล Sensitivity Study สามารถสรุปผลกระทบได้ดังนี้

\paragraph{ผลของการปรับ $Q$}
\begin{itemize}
    \item $Q \times 0.01$ ($q_1/R \approx 0.0003$): Filter เชื่อ Model อย่างสมบูรณ์
          Noise Reduction สูงถึง 90.2\% แต่ Lag Error จะมากขึ้นหาก
          Dynamics จริงเบี่ยงเบนจาก Model (``Overconfident Filter'')
    \item $Q \times 1$ (Baseline, $q_1/R \approx 0.030$): สมดุลระหว่าง
          Smoothness และ Responsiveness Noise Reduction 16.6\%
    \item $Q \times 100$ ($q_1/R \approx 2.97$): Filter ตาม Measurement
          เกือบสมบูรณ์ Noise Reduction ติดลบ ($-$0.7\%)
          แสดงว่า Filter ไม่มีประโยชน์ในกรณีนี้
\end{itemize}

\paragraph{ผลของการปรับ $R$}
\begin{itemize}
    \item $R \times 0.5$ ($q_1/R \approx 0.059$): บอก Filter ว่า Sensor
          แม่นกว่าความเป็นจริง Kalman Gain สูงขึ้น Filter ดึงตาม Noise
          มากขึ้น Noise Reduction ติดลบ ($-$0.9\%)
    \item $R \times 2$ ($q_1/R \approx 0.015$): Filter เชื่อ Model มากขึ้น
          Noise Reduction ใกล้เคียง Baseline (16.8\%)
          เพราะ $Q/R$ Ratio เปลี่ยนไปเพียง 2 เท่า
\end{itemize}

ข้อสังเกตสำคัญ: $Q$ มีอิทธิพลต่อพฤติกรรม KF มากกว่า $R$ อย่างชัดเจน
เพราะ $Q$ ถูกปรับในช่วง $\times 0.01$ ถึง $\times 100$ (10000 เท่า)
ในขณะที่ $R$ ปรับเพียง $\times 0.5$ ถึง $\times 2$ (4 เท่า)
สิ่งที่ควบคุมพฤติกรรม Filter จริงๆ คืออัตราส่วน $Q/R$
ไม่ใช่ค่าใดค่าหนึ่งอย่างเดียว

\subsubsection{ข้อจำกัดของระบบและแนวทางพัฒนา}

\begin{itemize}
    \item \textbf{Noise Floor ของ HC-SR04:} เซนเซอร์วัดได้ต่ำสุดประมาณ 2~cm
          ดังนั้น Oscillation ที่ Amplitude เล็กมากอาจถูก Noise กลบ
          ทำให้ KF ไม่สามารถประมาณ State ได้แม่นยำในช่วงท้าย
          ของ Free Vibration ที่ Amplitude ลดลงมาก

    \item \textbf{ข้อสมมติ Linear Model:} EOM สมมติว่า $K$ และ $C$
          คงที่และเป็น Linear ซึ่งอาจไม่สมบูรณ์ที่ Amplitude ใหญ่
          หากต้องการความแม่นยำสูงขึ้น ควรใช้ Extended Kalman Filter (EKF)
          กับ Nonlinear Model

    \item \textbf{Non-Adaptive Filter:} KF นี้ใช้ $Q$ และ $R$ คงที่ตลอดเวลา
          ซึ่งอาจไม่เหมาะสมหาก Noise Characteristics เปลี่ยนแปลงตามระยะ
          การพัฒนาต่อเป็น Adaptive KF จะช่วยให้ระบบ Tune ตัวเองได้

    \item \textbf{ความคลาดเคลื่อนจาก Discretization:}
          Euler Forward มี Truncation Error $O(T_s^2)$
          ที่ $T_s = 10$~ms ความต่างจาก ZOH ประมาณ 2\%
          ซึ่งยอมรับได้สำหรับ Application นี้แต่ควรระบุไว้ใน Limitation
\end{itemize}